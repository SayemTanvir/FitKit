import { useEffect, useState } from 'react';
import toast from 'react-hot-toast';
import { fetchExercises, saveExerciseLibraryItem, setExerciseActive } from '../services/api';
import ImageUploadField from '../components/ImageUploadField';
import { StoredImage } from '../components/UserAvatar';

const blank={name:'',description:'',category:'Strength',movement_pattern:'',target_muscle_group:'',secondary_muscles:'',equipment_needed:'',optional_equipment:'',difficulty_level:'Beginner',tracking_type:'reps',instructions:'',media_url:'',calorie_factor:0.3};

export default function ExerciseLibrary(){
  const [items,setItems]=useState<any[]>([]),[form,setForm]=useState(blank),[editing,setEditing]=useState<number|null>(null);
  const [search,setSearch]=useState(''),[category,setCategory]=useState('All'),[error,setError]=useState(''),[busy,setBusy]=useState(false),[uploadBusy,setUploadBusy]=useState(false);
  const load=()=>fetchExercises().then(setItems).catch((e)=>setError(e.message));
  useEffect(()=>{load();},[]);
  const edit=(item:any)=>{setEditing(item.exercise_id);setForm({name:item.name,description:item.description||'',category:item.category,movement_pattern:item.movement_pattern||'',target_muscle_group:item.target_muscle_group,secondary_muscles:(item.secondary_muscles||[]).join(', '),equipment_needed:item.equipment_needed||'',optional_equipment:item.optional_equipment||'',difficulty_level:item.difficulty_level,tracking_type:item.tracking_type,instructions:item.instructions||'',media_url:item.media_url||'',calorie_factor:Number(item.calorie_factor||0.3)});window.scrollTo({top:0,behavior:'smooth'});};
  const submit=async(event:React.FormEvent)=>{event.preventDefault();if(uploadBusy)return;setBusy(true);try{await saveExerciseLibraryItem({...form,secondary_muscles:form.secondary_muscles.split(',').map((s)=>s.trim()).filter(Boolean)},editing||undefined);toast.success(editing?'Exercise updated':'Exercise added');setEditing(null);setForm(blank);await load();}catch(e:any){toast.error(e.message);}finally{setBusy(false);}};
  const toggle=async(item:any)=>{try{await setExerciseActive(item.exercise_id,!item.is_active);await load();}catch(e:any){toast.error(e.message);}};
  const shown=items.filter((item)=>item.name.toLowerCase().includes(search.toLowerCase())&&(category==='All'||item.category===category));
  return <div className="space-y-6"><div className="glass rounded-2xl p-6"><h1 className="font-display text-2xl font-semibold text-white">Exercise Library</h1><p className="text-sm text-slate-400 mt-1">Reusable, approved exercises for programme prescriptions. Archived exercises remain in historical programmes.</p></div><form onSubmit={submit} className="glass rounded-2xl p-6 space-y-4"><h2 className="font-display text-lg font-semibold text-white">{editing?'Edit exercise':'New exercise'}</h2><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-3">{(['name','target_muscle_group','movement_pattern','equipment_needed','optional_equipment','secondary_muscles'] as const).map((field)=><label key={field} className="text-xs text-slate-400">{field.replaceAll('_',' ')}<input value={form[field]} onChange={(e)=>setForm((previous)=>({...previous,[field]:e.target.value}))} className="input-pro mt-1" required={field==='name'||field==='target_muscle_group'} /></label>)}<label className="text-xs text-slate-400">Category<select value={form.category} onChange={(e)=>setForm((p)=>({...p,category:e.target.value}))} className="input-pro mt-1"><option>Strength</option><option>Cardio</option><option>Flexibility</option></select></label><label className="text-xs text-slate-400">Difficulty<select value={form.difficulty_level} onChange={(e)=>setForm((p)=>({...p,difficulty_level:e.target.value}))} className="input-pro mt-1"><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></label><label className="text-xs text-slate-400">Track by<select value={form.tracking_type} onChange={(e)=>setForm((p)=>({...p,tracking_type:e.target.value}))} className="input-pro mt-1"><option value="reps">Reps and load</option><option value="time">Time</option><option value="distance">Distance</option></select></label><label className="text-xs text-slate-400">Calorie factor<input type="number" min="0.01" step="0.01" value={form.calorie_factor} onChange={(e)=>setForm((p)=>({...p,calorie_factor:Number(e.target.value)}))} className="input-pro mt-1" /></label></div><ImageUploadField label="Exercise demonstration image (optional)" value={form.media_url} onChange={(media_url)=>setForm((p)=>({...p,media_url}))} purpose="exercise" onBusyChange={setUploadBusy}/><label className="block text-xs text-slate-400">Description<textarea value={form.description} onChange={(e)=>setForm((p)=>({...p,description:e.target.value}))} className="input-pro mt-1 w-full min-h-16" /></label><label className="block text-xs text-slate-400">Instructions, technique and safety<textarea value={form.instructions} onChange={(e)=>setForm((p)=>({...p,instructions:e.target.value}))} className="input-pro mt-1 w-full min-h-20" /></label><div className="flex gap-2"><button type="submit" disabled={busy||uploadBusy} className="btn-primary rounded-xl px-5 py-2.5 text-sm font-semibold disabled:opacity-50">{busy?'Saving...':'Save exercise'}</button>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(blank);}} className="rounded-xl border border-white/10 px-4 text-sm text-slate-300">Cancel</button>}</div></form><div className="glass rounded-2xl p-6"><div className="flex flex-wrap gap-3 mb-4"><input aria-label="Search exercises" placeholder="Search exercises" value={search} onChange={(e)=>setSearch(e.target.value)} className="input-pro max-w-xs" /><select aria-label="Filter exercise category" value={category} onChange={(e)=>setCategory(e.target.value)} className="input-pro max-w-xs"><option>All</option><option>Strength</option><option>Cardio</option><option>Flexibility</option></select></div>{error&&<p className="text-red-300">{error}</p>}<div className="grid md:grid-cols-2 gap-3">{shown.map((item)=><div key={item.exercise_id} className="rounded-xl border border-white/10 p-4"><div className="flex justify-between gap-2"><h3 className="text-sm font-semibold text-white">{item.name}</h3><span className={`text-xs ${item.is_active?'text-lime-300':'text-amber-300'}`}>{item.is_active?'Active':'Archived'}</span></div><p className="text-xs text-slate-400 mt-1">{item.category} · {item.target_muscle_group} · {item.difficulty_level} · {item.tracking_type}</p><p className="text-xs text-slate-400 mt-2 line-clamp-2">{item.instructions}</p>{item.media_url&&<StoredImage url={item.media_url} alt={`${item.name} demonstration`} className="max-h-36 rounded-lg mt-2 object-cover"/>}<div className="flex gap-3 mt-3"><button type="button" onClick={()=>edit(item)} className="text-xs text-cyan-300">Edit</button><button type="button" onClick={()=>toggle(item)} className="text-xs text-amber-300">{item.is_active?'Archive':'Restore'}</button></div></div>)}</div></div></div>;
}
